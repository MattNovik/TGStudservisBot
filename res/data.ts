const FROM_TYPES: {
  all: string,
  catBot: string,
  after: string
} = {
  all: 'all',
  catBot: 'catBot',
  after: 'after',
};

const ACTION_SCENE_OUT: Array<string> = [
  '/start',
  '/manager',
  '/create'
]

export { FROM_TYPES, ACTION_SCENE_OUT };
